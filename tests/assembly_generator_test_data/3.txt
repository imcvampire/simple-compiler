var a = 8589934592
----------------
.extern print_int
.extern print_bool
.extern read_int
.global main
.type main, @function

.section .text

main:
pushq %rbp
movq %rsp, %rbp
subq $40, %rsp

.LStart:

# LoadIntConst(8589934592, v0)
movabsq $8589934592, %rax
movq %rax, -8(%rbp)

# Copy(v0, v1)
movq -8(%rbp), %rax
movq %rax, -16(%rbp)

# Call(print_int, [v1], v2)
movq -16(%rbp), %rdi
call print_int
movq %rax, -32(%rbp)

# Return()
movq $0, %rax
movq %rbp, %rsp
popq %rbp
ret
